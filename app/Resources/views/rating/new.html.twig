{% extends 'template/defaultTemplate.html.twig' %}

{% block page_title %}<title>Rating ADD </title>{% endblock %}

{% block content %}
    <h1>Rating creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_rating_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
