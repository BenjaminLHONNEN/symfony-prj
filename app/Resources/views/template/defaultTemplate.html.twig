<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">

    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"/>

    <link rel="stylesheet" href="{{ asset("asset/style/B.css") }}">
    <link rel="stylesheet" href="{{ asset("asset/style/style.css") }}">
    <link rel="stylesheet" href="{{ asset("asset/font-awesome/css/font-awesome.css") }}">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

    {% block page_title %}
        <title>Strategy Games Review</title>
    {% endblock %}
</head>
<body>

<header class="header-center deskop-header appear-fade">
    <div class="header-display-left">
        <ul class="header-button-left">
            <li><a href="{{ url('game_list') }}">Games List</a></li>
        </ul>
        {% if is_granted('ROLE_ADMIN') %}
        <ul class="header-button-left">
            <li><a href="{{ url('admin_dashboard') }}">Espace Admin</a></li>
        </ul>
        {% endif %}
    </div>
    <div class="header-display-right">
        <ul class="header-button-right">
            {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                <li>
                    <a href="{{ url('login') }}">
                        <i class="fa fa-sign-in"></i> Sign in
                    </a>
                </li>
                <li>
                    <a href="{{ url('signUp') }}">
                        <i class="fa fa-user-plus"></i> Sign up
                    </a>
                </li>
            {% else %}
                <li>
                    <a class="nav-link" href="{{ path('logout') }}">
                        <i class="fa fa-sign-out"></i> Se déconnecter ({{ app.user.username }})
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>

</header>

{% block content '' %}

</body>
</html>